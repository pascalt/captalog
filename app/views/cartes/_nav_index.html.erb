<%= link_to 'Menu',  menu_path %>

<% if @village.blank?%>
<%else%>
  <%if @village.actif %>
      | <%= link_to 'Villages', villages_path%>
  <%else%>
      | <%= link_to 'Villages désactivés', index_non_actifs_villages_path%>
  <%end%>
  | <%= link_to 'Village : ' + @village.nom, village_path(@village)%>
  <%if @village.actif %>
    | <%= link_to 'Nouvelle carte', new_village_carte_path(@village)%>
  <%end%>
<%end%>



<%if @existe_cartes_non_actives%>
   | <%= link_to 'Cartes désactivées', 
                 @village ? index_non_actives_village_cartes_path(@village) : index_non_actives_cartes_path%>
<%end%>

