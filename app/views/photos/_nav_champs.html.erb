<%= link_to 'Menu', menu_path %> |
<% if @photo.actif %>
  <%= link_to 'Photos', @village ? village_photos_path(@village) : photos_path %> |
<%else%>
 <%= link_to 'Photos désactivées', @village ? index_non_actives_village_photos_path(@photo.village) : index_non_actives_photos_path%> |
<%end%>
<% unless @photo.new_record?%>
  <%= link_to 'Voir', [@village, @photo]%>
<% end %>
