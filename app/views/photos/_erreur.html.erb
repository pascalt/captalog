<% if @photo.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@photo.errors.count, "erreur") %> ne permet<%= "tent" if @photo.errors.count > 1 %> pas d'enregistrer la photo:</h2>

    <ul>
    <% @photo.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

